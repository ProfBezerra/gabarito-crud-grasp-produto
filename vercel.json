{
  "buildCommand": "npm --prefix frontend/produtos-web install && npm --prefix frontend/produtos-web run build",
  "outputDirectory": "frontend/produtos-web/dist/produtos-web/browser",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
